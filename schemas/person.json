{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://example.com/schemas/person.json",
  "title": "Person",
  "description": "Person schema used for Codex archive validations and replay payloads.",
  "type": "object",
  "required": ["person_id", "full_name", "email"],
  "properties": {
    "person_id": {
      "type": "string",
      "description": "Stable unique identifier assigned to the person."
    },
    "full_name": {
      "type": "string",
      "minLength": 1,
      "description": "Concatenated display name."
    },
    "email": {
      "type": "string",
      "format": "email",
      "description": "Primary contact email."
    },
    "roles": {
      "type": "array",
      "description": "Roles granted to the person across Codex agents.",
      "items": {
        "type": "string"
      }
    },
    "metadata": {
      "type": "object",
      "description": "Optional metadata key/value pairs for integrations.",
      "additionalProperties": true
    }
  },
  "additionalProperties": false
}
